# Nunca al repositorio: credenciales corporativas (manual, paso 20)
.env
.env.*
!.env.example

node_modules/
npm-debug.log*
.DS_Store

# Sesión delegada: guarda un refresh token. Nunca al repositorio.
backend/.sesiones.json
backend/.sesiones.json.tmp

# El mapeo en uso es local: se siembra de modelo.ejemplo.json en el
# primer arranque. Versionarlo bloquearía el git pull al guardar cambios.
backend/modelo.json
